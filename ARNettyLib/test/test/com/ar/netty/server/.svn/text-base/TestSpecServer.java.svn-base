package test.com.ar.netty.server;

import org.junit.Test;

import com.ar.common.logging.ARLog;
import com.ar.netty.server.StartServer;

/********************************
 * <pre>
 * Spec 송수신 서버 테스트용 클래스.
 * </pre>
 * @author Bak Sang Heon
 *
 ********************************/
public class TestSpecServer {
	private static ARLog log = ARLog.getLogger(TestSpecServer.class);
	
	@Test
	public void test() {
		log.info("Start Spec Server");
		
		// TODO : Server start
		StartServer server = new StartServer();
		server.start();
		
		log.info("Start Spec Server Completed");
	} // test()
	
} // class TestSpecServer
